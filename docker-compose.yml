services:
  chatbot:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - LANGSMITH_TRACING=${LANGSMITH_TRACING}
      - LANGSMITH_PROJECT=${LANGSMITH_PROJECT}
    restart: unless-stopped
